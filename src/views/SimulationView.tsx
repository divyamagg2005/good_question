import React from 'react';

export const SimulationView: React.FC = () => {
  return (
    <div
      style={{
        width: '100%',
        height: '100%',
        background: '#090c10',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    />
  );
};
